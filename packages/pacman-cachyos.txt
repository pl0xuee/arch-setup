# Packages that only exist in the CachyOS repos.
#
# On a box without those repos — Omarchy is plain Arch — install.sh adds the
# signed [cachyos] repo rather than reaching for the AUR, so this list installs
# everywhere. See ensure_cachyos_repo() for why the repo is added BELOW
# core/extra/multilib instead of above it, the way CachyOS's own installer
# does it.

brave-origin-bin             # Brave browser
vesktop                      # Discord client
protonup-qt                  # Manage Proton-GE / Wine-GE versions

# Exactly what the "Install Gaming packages" button in CachyOS Hello installs.
# Most of what they pull in (steam, lutris, gamescope, mangohud, goverlay) comes
# from Arch's own extra/multilib; the CachyOS repo supplies the metapackages
# themselves plus proton-cachyos-slr, wine-cachyos-opt, umu-launcher and
# heroic-games-launcher-bin. multilib has to be enabled for steam and the lib32
# packages — ensure_cachyos_repo() turns it on if it isn't.
cachyos-gaming-meta
cachyos-gaming-applications
